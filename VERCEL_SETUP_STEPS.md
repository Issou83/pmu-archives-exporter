# 📝 Étapes de configuration Vercel

## Questions et réponses

Quand Vercel vous pose ces questions, voici les réponses à donner :

1. **"Set up and develop?"** → `yes` ✅ (déjà fait)

2. **"Which scope?"** → `issou83's projects` ✅ (déjà fait)

3. **"Link to it?"** → `no` ✅ (déjà fait)

4. **"Link to different existing project? (Y/n)"** → **`no`** (ou appuyez sur `n` puis Entrée)

5. **"What's your project's name?"** → Tapez `pmu-archives-exporter` puis Entrée

6. **"In which directory is your code located?"** → Appuyez simplement sur Entrée (c'est `./` par défaut)

7. **"Want to modify these settings?"** → `no` (ou appuyez sur `n` puis Entrée)

8. **"Do you want to change additional project settings?"** → `no` (ou appuyez sur `n` puis Entrée)

## Après la configuration

Une fois toutes les questions répondues, vous devriez voir :

```
🔗 Linked to [votre-projet] (created .vercel)

✅ Created .env.local file and added it to .gitignore

> Ready! Available at http://localhost:3000
```

Ensuite, votre application sera accessible sur `http://localhost:3000` avec les API routes fonctionnelles !

